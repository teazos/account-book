export interface MonthlySummary { income: number; expense: number; balance: number }
export interface CategorySummary { categoryId: string; categoryName: string; total: number }
export interface DailyTrend { transactionDate: string; type: string; total: number }
